import React from 'react';
import PropTypes from 'prop-types';
import styles from './auth.css';

function auth(parops{  }) {
  return (
    <div className={styles.base}>
      
    </div>
  );
}

auth.defaultProps = {};

auth.propTypes = {
};

export default auth;
